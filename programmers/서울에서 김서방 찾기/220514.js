function findKim(seoul) {
  var idx = seoul.indexOf('Kim');
  // 문자열에서 특정 문자열을 찾고, 검색된 문자열이 '첫번째'로 나타나는 위치 index를 리턴.
  return "김서방은 " + idx + "에 있다";
  // let index = seoul.indexOf('kim');
  // return "김서방은 "+ index +"에 있다."
}